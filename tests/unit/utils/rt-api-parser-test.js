import rtApiParser from '../../../utils/rt-api-parser';
import { module, test } from 'qunit';

module('Unit | Utility | rt api parser');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = rtApiParser();
  assert.ok(result);
});
