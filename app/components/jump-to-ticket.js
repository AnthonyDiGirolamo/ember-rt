import Ember from 'ember';

export default Ember.Component.extend({
    actions: {
        // jumpToTicket: function() {
        //     var ticketid = this.getProperties('ticket_id');
        //     console.log("TICKETID:");
        //     console.log(ticketid);
        //     this.sendAction('action', this.getProperties('ticket_id'));
        // }
    }
});
